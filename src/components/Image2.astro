---
export interface Props {
  src: string;
  alt?: string;
  title?: string;
  class?: string;
  width?: string;
  height?: string;
}
const {src, alt = '', title, class, width, height} = Astro.props;
---
<div src={src} alt={alt} title={title} class={class} width={width} height={height} />
