---
import getPaths from '../../lib/getPaths.js';
import NotFound from '../../templates/404.astro';

export async function getStaticPaths() {
  return getPaths();
}

const { locale } = Astro.request.params;
---

<NotFound {locale} />
